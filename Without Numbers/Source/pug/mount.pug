fieldset.repeating_mount
	.core-box.stats
		.grid.grid-flow-col
			each val in ["name", "type"]
				+text-input(val, val.toUpperCase())
		.grid.grid-flow-col
			each val in ["ac", "hp", "morale", "spd"]
				+text-input(val, val.toUpperCase())
		.grid.grid-flow-col  
			each val in ["attack", "carrying_capacity"]
				+text-input(val, val.toUpperCase())
		.grid.mt
			+sectionheader("show_gear", "GEAR")
			div
				textarea(name=`attr_gear`)
