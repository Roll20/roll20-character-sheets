include ../../components/settings/menu

mixin settingsPage(sheetType)
  div.page-container
    input(type='radio' name=`attr_${sheetType}_page` class='settings hide-element' value='9')
    div.settings.showhide
      +settings