rolltemplate.sheet-rolltemplate-anima
  .sheet-template-container
    .sheet-template-header
      | {{#character}}
      span.sheet-template-character {{character}}
      | {{/character}}
      | Fait appel à son âme
    .sheet-template-body
      | {{#jet}}
      span {{jet}}
      | {{/jet}}
